import React from "react";

export function AllContestsTable() {
  return <div>AllContestsTable</div>;
}

export default {
  AllContestsTable,
};

// Build a reusable component to display contests, regardless of which tab we're on, in a table form
